FROM node:18-alpine

WORKDIR /app

EXPOSE 5173

# Perintah ini hanya untuk menjaga agar kontainer tetap berjalan.
# Server Vite (npm run dev) akan kita jalankan nanti secara manual
# setelah proyek React dibuat oleh Breeze.
CMD ["tail", "-f", "/dev/null"]